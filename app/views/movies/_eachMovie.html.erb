<div class="row">
  <% if current_user.follows?(@movie) %>
    <div class="col-md-2" style="margin-bottom: 30px">
        <%= link_to "Remove", remove_movie_path(@movie) %> | <%= @movie.followers_count %> followers
        <%= image_tag @movie.image %>
        <h3><%= @movie.title %></h3>
        <p><%= @movie.release_year %></p>
        <p><%= @movie.plot %></p>
    </div>
  <% else %>
    <div class="col-md-2" style="margin-bottom: 30px">
        <%= link_to "Add", add_movie_path(@movie) %> | <%= @movie.followers_count %> followers
        <%= image_tag @movie.image %>
        <h3><%= @movie.title %></h3>
        <p><%= @movie.release_year %></p>
        <p><%= @movie.plot %></p>
    </div>
  <% end %>
</div>